/**
 * Handlgin file uploads. Upload an image
 * register the input.
 * add prop in styled component: type="file" {...register}
 *Verify in console the type of data. specify the image name and the path to the image in the bucket.
Check the default documentation of Supabase, and add policies to the bucket
Grab the cocde, change the bucket name, and image name
 * ***/
